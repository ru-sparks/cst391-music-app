export default function Home() {
  return (
    <main style={{padding: 24}}>
      <h1>Sparks Music API</h1>
      <p>Try <code>/api/albums</code>.</p>
      <p>Try <code>/api/db-check</code>.</p>
      <p>Use the <code>/api/*</code> routes. Try <code>/api/albums</code>.</p>
    </main>
  );
}
